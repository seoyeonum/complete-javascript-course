// constant(상수)의 경우, 변수명을 대문자로 작성!
// 일반적으로 URL은 '/'로 끝난다.
export const API_URL = 'https://forkify-api.jonas.io/api/v2/recipes';
export const TIMEOUT_SEC = 10;
export const RES_PER_PAGE = 10;
export const MODAL_CLOSE_SEC = 2.5;

// ※ 현재 API KEY 가 서버 문제로 작동하지 않으니,
// 이후 정상 발급 가능해진다면
// <my key from API>에 발급받은 key 넣고 추후 꼭 확인해볼것!!
export const KEY = '<my key from API>';
